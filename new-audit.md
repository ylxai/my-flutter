# Flutter Desktop Native Audit Report

## CRITICAL ISSUES (Must Fix Before Release)

### 1. Panic Propagation Across FFI Boundary
- **Location:** All functions in `rust/src/api.rs:99-359`
- **Detected language:** Rust
- **Issue:** No `std::panic::catch_unwind` wrapper. Any panic (e.g., from `unwrap()`, array bounds, allocation failure) will cross FFI boundary causing undefined behavior and application crash.
- **Native error path:** Any internal panic
- **Dart propagation:** None - application crash
- **Severity:** CRITICAL
- **Status:** FIXED
- **Resolution:** Semua fungsi API publik sudah dibungkus `std::panic::catch_unwind` dan dialihkan ke fungsi `_inner` agar panic tidak melewati batas FFI. Fallback aman disediakan saat panic terjadi.
- **Fix (implemented):**
```rust
pub fn copy_single_file(
    source: String,
    destination: String,
    skip_existing: bool,
) -> NativeFileCopyResult {
    std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        copy_single_file_inner(source, destination, skip_existing)
    }))
    .unwrap_or_else(|_| NativeFileCopyResult {
        source_path: source,
        dest_path: destination,
        bytes_copied: 0,
        duration_ms: 0,
        speed_mbps: 0.0,
        strategy_used: "Panic".to_string(),
        success: false,
        error_message: "Internal panic - operation failed".to_string(),
        skipped: false,
    })
}

fn copy_single_file_inner(
    source: String,
    destination: String,
    skip_existing: bool,
) -> NativeFileCopyResult {
    // Original implementation here
}
```

---

### 2. Race Condition on Global Pause/Cancel Flags
- **Location:** `rust/src/api.rs:91-94` and `rust/src/api.rs:142-144, 193-205`
- **Detected language:** Rust
- **Issue:** Static `CANCEL_FLAG` and `PAUSE_FLAG` use `Ordering::Relaxed`. Concurrent calls to `copy_files_batch` from different Dart isolates or rapid successive calls will corrupt state. Line 142-143 resets flags tanpa sinkronisasi dengan operasi yang sedang berjalan.
- **Crash or race condition risk:** HIGH - Data corruption, stuck operations, incorrect cancellation, operations that cannot be cancelled
- **Severity:** CRITICAL
- **Status:** FIXED
- **Resolution:** Mengganti flag global menjadi token per operasi lewat `CURRENT_OPERATION` dan `OperationFlags`. Semua operasi pause/cancel menggunakan `Ordering::SeqCst` dan hanya berlaku untuk batch aktif.
- **Fix (implemented):**
```rust
#[derive(Debug)]
struct OperationFlags {
    cancel: Arc<AtomicBool>,
    pause: Arc<AtomicBool>,
}

static CURRENT_OPERATION: std::sync::LazyLock<Mutex<Option<Arc<OperationFlags>>>> =
    std::sync::LazyLock::new(|| Mutex::new(None));

fn copy_files_batch_inner(...) -> NativeBatchResult {
    let op_flags = Arc::new(OperationFlags {
        cancel: Arc::new(AtomicBool::new(false)),
        pause: Arc::new(AtomicBool::new(false)),
    });

    {
        let mut current = CURRENT_OPERATION
            .lock()
            .expect("CURRENT_OPERATION mutex poisoned");
        if let Some(existing) = current.as_ref() {
            existing.cancel.store(true, Ordering::SeqCst);
        }
        *current = Some(Arc::clone(&op_flags));
    }

    // ... gunakan op_flags.cancel & op_flags.pause
}

pub fn cancel_copy() {
    if let Some(current) = CURRENT_OPERATION.lock().expect("CURRENT_OPERATION mutex poisoned").as_ref() {
        current.cancel.store(true, Ordering::SeqCst);
    }
}
```

---

### 3. String::from_utf8().unwrap() Panic in Deserializer
- **Location:** `rust/src/frb_generated.rs:170`
- **Detected language:** Rust (generated by flutter_rust_bridge)
- **Issue:** `String::from_utf8(inner).unwrap()` panics on invalid UTF-8 bytes from Dart. This is generated code that cannot be directly modified.
- **Native error path:** Invalid UTF-8 bytes passed from Dart
- **Dart propagation:** None - application crash
- **Severity:** HIGH
- **Status:** MITIGATED
- **Resolution:** Semua API publik sudah dibungkus `catch_unwind` sehingga panic dari kode generated tidak melintasi batas FFI.
- **Fix:** Ini kode generated; mitigasi dilakukan lewat wrapper `catch_unwind`. Perbaikan permanen perlu upstream flutter_rust_bridge.
  1. **Immediate:** Wrap all API calls with catch_unwind (see Critical Issue #1)
  2. **Long-term:** File issue with flutter_rust_bridge to use `from_utf8_lossy` or return Result

---

### 4. .unwrap() on All Decode Operations in Generated Code
- **Location:** `rust/src/frb_generated.rs:175, 180, 284, 290, 294, 304, 453, 458, etc.`
- **Detected language:** Rust (generated by flutter_rust_bridge)
- **Issue:** All decode operations use `.unwrap()` - will panic on malformed data, buffer underflow, or corrupted transmission.
- **Evidence:** Lines like:
  ```rust
  fn sse_decode_u_32(...) -> Self { 
      deserializer.cursor.read_u32::<NativeEndian>().unwrap() 
  }
  ```
- **Panic / abort / silent-failure risk:** HIGH
- **Severity:** HIGH
- **Status:** MITIGATED
- **Resolution:** Wrapper `catch_unwind` pada API publik mencegah panic melewati FFI. Perlu upstream untuk perbaikan permanen.
- **Fix:** Generated code - upstream issue required. Immediate mitigation with catch_unwind wrapper on all API functions.

---

## HIGH SEVERITY ISSUES

### 5. Unsafe Memory-Mapped I/O Without Panic Safety
- **Location:** `rust/src/file_copy.rs:181-193, 223-241`
- **Detected language:** Rust
- **Issue:** `unsafe { MmapOptions::new().map(&src_file)? }` can panic on alignment errors or unexpected conditions. If panic occurs inside unsafe block during FFI call, undefined behavior results.
- **Allocation source:** Native (Rust)
- **Ownership model:** RAII - Mmap owns the mapping, dropped when out of scope
- **Drop/free correctness:** Correct - memmap2 handles munmap
- **Leak / UAF / double-free risk:** LOW under normal operation, CRITICAL if panic crosses FFI
- **Severity:** HIGH
- **Status:** FIXED
- **Resolution:** Operasi `mmap` sekarang dibungkus `catch_unwind` untuk mencegah panic melintasi batas FFI, baik pada copy mmap biasa maupun chunked.
- **Fix (implemented):**
```rust
fn copy_memory_mapped(src: &Path, dst: &Path) -> io::Result<u64> {
    // ... setup code ...
    
    let mmap = std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        unsafe { MmapOptions::new().map(&src_file) }
    }))
    .map_err(|_| io::Error::new(io::ErrorKind::Other, "Panic during memory map"))??;
    
    // ... rest of implementation ...
}
```

---

### 6. Rayon Thread Pool Created Per Batch Call
- **Location:** `rust/src/parallel.rs:71-74`
- **Detected language:** Rust
- **Issue:** Each `copy_files_parallel` call creates a new ThreadPool. No reuse, potential thread exhaustion under load, and overhead of thread creation.
- **Evidence:** 
  ```rust
  let pool = rayon::ThreadPoolBuilder::new()
      .num_threads(max_threads)
      .build()
      .unwrap_or_else(|_| rayon::ThreadPoolBuilder::new().build().unwrap());
  ```
- **Crash or race condition risk:** MEDIUM - Resource exhaustion, performance degradation
- **Severity:** HIGH
- **Fix:** Use global thread pool or rayon's default:
```rust
pub fn copy_files_parallel(
    files: Vec<FileEntry>,
    max_threads: usize,
    // ... other params
) -> BatchCopyResult {
    // Configure global pool once at startup, or use default
    let results: Vec<FileCopyResult> = files
        .par_iter()
        .with_max_len(max_threads)
        .map(|entry| {
            // ... copy logic ...
        })
        .collect();
}
```

---

### 7. Busy-Wait Pause Implementation
- **Location:** `rust/src/parallel.rs:96-101`
- **Detected language:** Rust
- **Issue:** Pause uses spin-loop with 100ms sleep. Wastes CPU and can miss cancel signal.
- **Evidence:** 
  ```rust
  while pause_flag.load(Ordering::Relaxed) {
      if cancel_flag.load(Ordering::Relaxed) {
          break;
      }
      std::thread::sleep(std::time::Duration::from_millis(100));
  }
  ```
- **Crash or race condition risk:** LOW - Performance/CPU waste
- **Severity:** MEDIUM
- **Fix:** Use `std::sync::Condvar` or `parking_lot::Condvar`:
```rust
use std::sync::{Arc, Condvar, Mutex};

pub struct PauseController {
    paused: Mutex<bool>,
    condvar: Condvar,
}

impl PauseController {
    pub fn wait_while_paused(&self, cancel_flag: &AtomicBool) {
        let mut paused = self.paused.lock().unwrap();
        while *paused && !cancel_flag.load(Ordering::SeqCst) {
            paused = self.condvar.wait(paused).unwrap();
        }
    }
    
    pub fn set_paused(&self, value: bool) {
        let mut paused = self.paused.lock().unwrap();
        *paused = value;
        if !value {
            self.condvar.notify_all();
        }
    }
}
```

---

## MEDIUM SEVERITY ISSUES

### 8. No Path Traversal Protection
- **Location:** `rust/src/api.rs:272-319` (scan_directory), `rust/src/file_copy.rs:55-137` (copy_file)
- **Detected language:** Rust
- **Issue:** Paths from Dart are used directly without sanitization. Symlink following is disabled (`follow_links(false)`) but path traversal (`../`) is not validated.
- **Evidence:** `Path::new(&source)` - no canonicalization or validation
- **Vector:** Malicious Dart code or compromised input could read/copy arbitrary files
- **Severity:** MEDIUM (local application, no network exposure)
- **Fix:** Canonicalize and validate paths:
```rust
use std::path::PathBuf;

fn validate_path(path: &Path, allowed_base: &Path) -> io::Result<PathBuf> {
    let canonical = path.canonicalize()
        .map_err(|e| io::Error::new(io::ErrorKind::InvalidInput, 
            format!("Invalid path: {}", e)))?;
    
    if !canonical.starts_with(allowed_base) {
        return Err(io::Error::new(
            io::ErrorKind::PermissionDenied, 
            "Path outside allowed directory"
        ));
    }
    
    Ok(canonical)
}

pub fn scan_directory(path: String, extensions: Vec<String>) -> Vec<NativeFileEntry> {
    let dir = Path::new(&path);
    
    // Validate path is accessible
    let canonical_dir = match dir.canonicalize() {
        Ok(p) => p,
        Err(_) => return Vec::new(),
    };
    
    // ... rest of implementation using canonical_dir
}
```

---

### 9. Serialization Overhead on Large Batches
- **Location:** All FFI calls via SseCodec in `rust/src/frb_generated.rs`
- **Detected language:** Rust/Dart
- **Overhead source:** Entire batch result serialized to bytes, deserialized on Dart side
- **Evidence:** `rust/src/frb_generated.rs` - all `Vec<NativeFileCopyResult>` serialized element-by-element
- **Severity:** MEDIUM (Performance)
- **Fix:** Use streaming results or DcoCodec:
```rust
// Option 1: Use DcoCodec (more efficient)
flutter_rust_bridge::frb_generated_boilerplate!(
    default_stream_sink_codec = DcoCodec,
    default_rust_opaque = RustOpaqueMoi,
    default_rust_auto_opaque = RustAutoOpaqueMoi,
);

// Option 2: Stream results instead of batching
pub fn copy_files_stream(
    files: Vec<NativeFileEntry>,
    // ... params
) -> impl Stream<Item = NativeFileCopyResult> {
    // Use StreamSink from flutter_rust_bridge
}
```

---

### 10. Debug Library Path in Production Config
- **Location:** `lib/src/rust/frb_generated.dart:69-70`
- **Detected language:** Dart
- **Issue:** `ioDirectory: 'rust/target/release/'` - hardcoded path that may not exist or be incorrect
- **Evidence:** 
  ```dart
  ExternalLibraryLoaderConfig(
    stem: 'filecopy_native',
    ioDirectory: 'rust/target/release/',
    webPrefix: 'pkg/',
  )
  ```
- **Risk:** Incorrect in debug builds, broken if directory structure changes
- **Severity:** MEDIUM
- **Fix:** Use environment-relative path or flutter_rust_bridge's asset bundling:
```dart
static const kDefaultExternalLibraryLoaderConfig =
    ExternalLibraryLoaderConfig(
      stem: 'filecopy_native',
      ioDirectory: 'rust/target/${bool.fromEnvironment('dart.vm.product') ? 'release' : 'debug'}/',
      webPrefix: 'pkg/',
    );
```

---

## LOW SEVERITY ISSUES

### 11. Missing `#[repr(C)]` on FFI-exposed Structs
- **Location:** `rust/src/api.rs:18-87` (NativeFileCopyResult, NativeBatchResult, etc.)
- **Detected language:** Rust
- **Issue:** All DTO structs lack `#[repr(C)]`. While flutter_rust_bridge uses serialization (SseCodec), not direct memory layout, this affects potential future direct FFI access and documentation clarity.
- **Impact:** LOW - No immediate crash risk due to serialization layer
- **Severity:** LOW
- **Fix:** Add `#[repr(C)]` to all structs:
```rust
#[repr(C)]
#[derive(Debug, Clone)]
pub struct NativeFileCopyResult {
    pub source_path: String,
    pub dest_path: String,
    pub bytes_copied: u64,
    pub duration_ms: u64,
    pub speed_mbps: f64,
    pub strategy_used: String,
    pub success: bool,
    pub error_message: String,
    pub skipped: bool,
}
```

---

### 12. Unused Extern Block in Linux Platform Code
- **Location:** `rust/src/platform/linux.rs:89-98`
- **Detected language:** Rust
- **Issue:** The `copy_file_range` extern block is declared but unused. The actual call uses `libc::copy_file_range` which is correct.
- **Evidence:** Lines 89-98 declare extern but line 47-56 uses `libc::copy_file_range`
- **Severity:** LOW (dead code)
- **Fix:** Remove unused extern block:
```rust
// Remove lines 89-98
// The libc crate provides the correct binding
```

---

### 13. Vec<u8> Allocation on Hot Path
- **Location:** `rust/src/hash.rs:40, 57` and `rust/src/file_copy.rs:152`
- **Detected source:** Native (Rust)
- **Issue:** Buffer allocated inside loop iteration
- **Ownership model:** Owned Vec, correctly freed
- **Leak / UAF / double-free risk:** None
- **Impact:** Performance only - allocation per read loop iteration
- **Severity:** LOW
- **Fix:** Reuse buffer or use stack-allocated buffer:
```rust
fn compute_sha256(path: &Path) -> io::Result<String> {
    let file = File::open(path)?;
    let mut reader = BufReader::with_capacity(HASH_BUFFER_SIZE, file);
    let mut hasher = Sha256::new();
    
    // Reuse buffer
    let mut buf = vec![0u8; HASH_BUFFER_SIZE];
    
    loop {
        let n = reader.read(&mut buf)?;
        if n == 0 {
            break;
        }
        hasher.update(&buf[..n]);
    }
    
    Ok(format!("{:x}", hasher.finalize()))
}
```

---

### 14. Windows Platform Implementation Not Optimized
- **Location:** `rust/src/platform/windows.rs:11-20`
- **Detected language:** Rust
- **Issue:** Windows implementation just calls `fs::copy` - no mmap or platform-specific optimization
- **Evidence:** `fs::copy(src, dst)` - standard library copy
- **Severity:** LOW (Performance)
- **Fix:** Implement optimized Windows copy using Win32 API:
```rust
use windows::Win32::Storage::FileSystem::{
    CopyFileExW, PROGRESS_CANCEL, COPY_FILE_FLAGS
};

pub fn copy_file_win32(src: &Path, dst: &Path) -> io::Result<u64> {
    let src_wide: Vec<u16> = src.as_os_str().encode_wide().chain(Some(0)).collect();
    let dst_wide: Vec<u16> = dst.as_os_str().encode_wide().chain(Some(0)).collect();
    
    unsafe {
        let success = CopyFileExW(
            PCWSTR(src_wide.as_ptr()),
            PCWSTR(dst_wide.as_ptr()),
            None,  // progress callback
            None,  // callback data
            None,  // cancel flag
            COPY_FILE_FLAGS(0),
        );
        
        if !success.as_bool() {
            return Err(io::Error::last_os_error());
        }
    }
    
    fs::metadata(dst).map(|m| m.len())
}
```

---

### 15. DLL/SO Loading from Predictable Path
- **Location:** `lib/src/rust/frb_generated.dart:68-72`
- **Detected language:** Dart
- **Issue:** Native library loaded from `rust/target/release/` with no integrity verification
- **Vector:** DLL planting if attacker can write to that directory
- **Severity:** LOW (requires local access)
- **Fix:** Use absolute path resolution or verify library signature

---

### 16. No ABI Stability Guarantees Across Rust Versions
- **Location:** Build configuration
- **Issue:** Rust doesn't guarantee stable ABI. Recompiling with different Rust version could break FFI compatibility.
- **Risk:** LOW - Same Rust version used for Dart FFI generation
- **Fix:** Document Rust version requirement with `rust-toolchain.toml`:
```toml
# rust-toolchain.toml
[toolchain]
channel = "1.75.0"
components = ["rustfmt", "clippy"]
```

---

## EXECUTIVE SUMMARY

- **Overall risk score (1-10):** 6
- **Detected native languages:** Rust (flutter_rust_bridge 2.9.0)
- **Native safety level:** RISKY
- **Top 3 critical native risks:**
  1. Panic propagation across FFI boundary (no `catch_unwind` wrapper)
  2. Race condition on global pause/cancel flags with `Ordering::Relaxed`
  3. Generated code uses `.unwrap()` on all decode operations

- **Top 3 highest-ROI fixes:**
  1. Wrap all FFI-exposed functions with `std::panic::catch_unwind`
  2. Replace global static flags with per-operation cancellation tokens
  3. Add path canonicalization and validation

---

## PRODUCTION VERDICT

### First Likely Native Failure:
**Panic during memory-mapped copy** - If mmap fails unexpectedly or an allocation panics, the application will crash with undefined behavior. No recovery possible.

### Must-Fix Before Release:
1. Add `std::panic::catch_unwind` wrapper around all FFI-exposed functions
2. Replace `Ordering::Relaxed` with `Ordering::SeqCst` or use proper mutex for pause/cancel state
3. Validate and canonicalize file paths before operations
4. Fix race condition on global state

### Acceptable Risks:
- Missing `#[repr(C)]` - serialization layer protects ABI
- Busy-wait pause - functional, just inefficient
- Per-call ThreadPool creation - acceptable for typical batch sizes
- Windows lack of mmap optimization - functional, just slower
- Low-severity performance issues
